import "./admin-global.css";

export const metadata = {
  title: "Admin — V Design Fountains",
  robots: "noindex, nofollow",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
